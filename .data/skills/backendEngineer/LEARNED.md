# Learned Conventions

> This file is auto-updated by the AI when user corrections reveal conventions.
> Each entry has a date and the rule learned. Do NOT delete entries — they accumulate over time.
> Format: `- YYYY-MM-DD: rule description`

## Corrections

<!-- AI writes here when user corrects generated code -->

## Preferences

<!-- AI writes here when user states a preference -->

## Discovered Conventions

<!-- AI writes here when it discovers implicit project conventions through analysis -->

- 2026-07-01: Strings use double quotes in this codebase (~1,370 double-quoted vs ~50 single-quoted literals in `src/skillnir/`). Black `-S` preserves existing quotes — it does not enforce single quotes. SKILL.md's "single quotes preferred" rows are stale; follow the surrounding file's double-quote style until SKILL.md is regenerated.
- 2026-07-01: `cli.py` has no argparse subparsers and no `build_parser()` — all 25 commands are entries in the `choices=[...]` list of the single positional `command` argument in `main()`, handled by zero-arg, underscore-prefixed functions (`_my_command()`) dispatched via `elif`. SKILL.md's Common Recipes "Add a new CLI command" line and its Architecture "Data flow" line were rewritten directly to match this on 2026-08-02 (were "Add subparser in `cli.py`" / "CLI input → argparse subparser → ..."); `references/patterns.md`'s "CLI Command Pattern" code block, which showed a fabricated `_setup_parsers(subparsers)` / `subparsers.add_parser(...)` shape, was also rewritten to the real `choices=[...]` + `elif` shape the same day.
- 2026-08-02: `cli.py`'s `choices=[...]` list now has 28 commands (grew from 25): added `harness-research`, `software-research`, `cleanup-articles`, `package-vulns`, `news`. **Correction (verification pass)**: this enumeration was wrong — `software-research` and `news` were added in earlier commits (24→25 and 18→24 respectively, alongside `generate-wiki`/`delete-wiki`/`compress-docs`/`optimize-docs`/`testing-research`), not in the 25→28 step. The actual 25→28 additions were only `harness-research`, `cleanup-articles`, and `package-vulns` (cross-checked by extracting `src/skillnir/cli.py` at each intermediate commit — `c5520ce` had 25 commands, `4c980f2` had 28, and the 3-command diff between them is exactly those three).
- 2026-08-02: `pyproject.toml`'s dev floor is `pytest>=9.0.3`, not `9.0.2+` as SKILL.md's Testing Standards table previously said; fixed directly in SKILL.md on 2026-08-02, this entry kept for history.
- 2026-08-02: `cli.py` is now 2,224 lines and `TOOLS` in `tools.py` has 37 entries (also, `AITool` has no `.score` property/scoring mechanism anywhere in `tools.py` — SKILL.md's Architecture tree previously said "1,287 lines" / "32+ tools with scoring"; fixed directly in SKILL.md on 2026-08-02, this entry kept for history).
- 2026-08-02: SKILL.md's Architecture tree previously listed ~19 of the 33 modules now in `src/skillnir/` (verified via `ls src/skillnir/*.py | wc -l` — not 37, a figure that had leaked in from `tools.py`'s unrelated "37 tools" count); it predated `benchmarks.py`, `compressor.py`, `crypto.py`, `docs_compressor.py`, `docs_optimizer.py`, `notifications/`, `notifier.py`, `security.py`, `harness_researcher.py`, `software_researcher.py`, `testing_researcher.py`, `news.py`, `package_vulns.py`, `skill_validator.py`, `article_cleanup.py`, `article_status.py`, `usage.py`, and `wiki_generator.py` (18 modules, not the ~17 an earlier pass of this file enumerated — `usage.py` had been left off the list) — all present today. SKILL.md's tree now carries a footnote listing them; fixed directly on 2026-08-02, this entry kept for history.
- 2026-08-02: Fresh full-package audit found `references/patterns.md` and `references/test-patterns.md` had drifted much further than SKILL.md itself — several "full code examples" no longer matched the real functions they claim to document (none of this was previously flagged anywhere). All rewritten and, where feasible, verified by actually running the corrected example against the real source in a scratch pytest file: (a) `inject_skill()` takes `(project_root, skill, tools: list[AITool])` and returns `list[InjectionResult]` — patterns.md/test-patterns.md showed a single-tool `(skill_path, tool_dir) -> InjectionResult` shape that doesn't exist; (b) `AITool` has no `.score` property or any scoring mechanism anywhere in `tools.py` (SKILL.md's tree comment "with scoring" was also removed); (c) `sync_skill()` takes `(source_dir, target_dir, skill_name)` — source_dir/target_dir are the parent `.data/skills/`-style dirs, not individual skill paths as patterns.md showed; (d) `generate_docs_sdk()` takes `(target_project, prompt_text, on_progress=None) -> GenerationResult` (no `model` arg, Claude-only) and streams `claude_agent_sdk` typed messages (`AssistantMessage`/`TextBlock`/`ToolUseBlock`/`ResultMessage` via `isinstance`) — not a generic `message.type`/`.content` shape; (e) the raw `Popen` + stderr/stdout-draining-thread logic lives once in `backends.run_streaming_command()`, not inlined in `generate_docs_subprocess()`; (f) `detect_tools()` is a one-line `(target_root / tool.dotdir).is_dir()` check over every `TOOLS` entry — it does not call `shutil.which()` at all (that's used elsewhere, for backend-CLI availability, not AITool detection), so SKILL.md's "add detection pattern in `detect_tools()`" recipe was also corrected; (g) `load_config()` takes no path argument (reads the module-level `CONFIG_FILE` constant) and never returns `None` — it returns a default `AppConfig`.
