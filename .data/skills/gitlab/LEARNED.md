# Learned Conventions

> This file is auto-updated by the AI when user corrections reveal conventions.
> Each entry has a date and the rule learned. Do NOT delete entries — they accumulate over time.
> Format: `- YYYY-MM-DD: rule description`

## Corrections

## Preferences

## Discovered Conventions

<!-- Record per-repo facts as you learn them, e.g.:
- YYYY-MM-DD: This project's target branch is <branch>; branches are <type>/<KEY>-<n>-<slug>.
- YYYY-MM-DD: Commit subject style here is [<KEY>-<n>] Title.
- YYYY-MM-DD: MR template is at .gitlab/merge_request_templates/<name>.md.
- YYYY-MM-DD: glab <version> uses flag <x> for <purpose> (confirmed via glab mr create --help).
- YYYY-MM-DD: The linked Jira review status is "<Status>". -->
